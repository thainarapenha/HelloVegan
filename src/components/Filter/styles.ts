import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  imgFilter: {
    width: 30,
    height: 30,
    padding: 20,
    borderRadius: 4,
    backgroundColor: 'orange',
  },
});