import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  TitleContainer: {
    flexDirection: 'row',
    justifyContent: 'center',
  },
  textTitle: {
    fontSize: 24,
    fontWeight: 'bold',
  },
  img: {
    width: 30,
    height: 30,
  },
});